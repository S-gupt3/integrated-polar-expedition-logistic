async function initAnalytics() { const data = await getAnalytics(); new Chart(document.getElementById('utilizationChart'), { type: 'bar', data: { labels: ['Maitri', 'Bharati', 'Himadri'], datasets: [{ label: 'Utilization %', data: data.utilization, backgroundColor: ['#319f7b', '#5ab593', '#8ac8ac'], borderRadius: 5 }] }, options: { plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true, max: 100 } } } }); new Chart(document.getElementById('analyticsHealthChart'), { type: 'doughnut', data: { labels: ['Operational', 'Maintenance', 'Damaged', 'Missing'], datasets: [{ data: [82, 9, 5, 2], backgroundColor: ['#64c59b', '#efb35a', '#e87968', '#8a9aa2'], borderWidth: 0 }] }, options: { cutout: '65%', plugins: { legend: { position: 'bottom', labels: { boxWidth: 8, font: { size: 10 } } } } } }); new Chart(document.getElementById('analyticsConsumptionChart'), { type: 'line', data: { labels: data.consumption.map((item) => item.day), datasets: [{ label: 'Usage volume', data: data.consumption.map((item) => item.value), borderColor: '#d18d2e', backgroundColor: '#fff0d7', fill: true, tension: .35 }] }, options: { plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true } } } }); }
initAnalytics();
