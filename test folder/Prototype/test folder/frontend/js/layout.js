const currentPage = document.body.dataset.page || 'dashboard';
const navItems = [{ href: 'index.html', page: 'dashboard', label: 'Dashboard' }, { href: 'assets.html', page: 'assets', label: 'Assets' }, { href: 'inventory.html', page: 'inventory', label: 'Inventory' }, { href: 'analytics.html', page: 'analytics', label: 'Analytics' }];
const operations = typeof OPERATIONS === 'undefined' ? [{ id: 'isea-46', label: 'ISEA-46 / Maitri' }] : OPERATIONS;
const activeOperation = localStorage.getItem('polar-ops-active-operation') || operations[0].id;
document.getElementById('app-shell').innerHTML = `<aside class="sidebar"><div class="brand"><span class="brand-mark">*</span><div><strong>POLAR OPS</strong><small>NCPOR COMMAND</small></div></div><label class="workspace"><span>ACTIVE OPERATION</span><select id="operation-selector" aria-label="Select active operation">${operations.map((operation) => `<option value="${operation.id}" ${operation.id === activeOperation ? 'selected' : ''}>${operation.label}</option>`).join('')}</select></label><p class="nav-label">Command center</p><nav>${navItems.map((item) => `<a class="nav-item ${currentPage === item.page ? 'active' : ''}" href="${item.href}">${item.label}</a>`).join('')}</nav><div class="nav-divider"></div><p class="nav-label">Operations</p><a class="nav-item" href="#">Expeditions <small>Shiv</small></a><a class="nav-item" href="#">Logistics <small>Shiv</small></a><a class="nav-item" href="#">Maintenance <small>Shiv</small></a><div class="sidebar-footer">Aryan Rao / Expedition manager</div></aside><button class="mobile-toggle" aria-label="Open menu">Menu</button>`;
document.getElementById('operation-selector').addEventListener('change', (event) => { localStorage.setItem('polar-ops-active-operation', event.target.value); window.location.reload(); });
document.querySelector('.mobile-toggle').addEventListener('click', () => document.querySelector('.sidebar').classList.toggle('open'));
