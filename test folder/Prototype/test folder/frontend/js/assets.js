async function initAssets() { const tbody = document.getElementById('assets-tbody'); if (!tbody) return; let allAssets = await getAssets(); const render = () => { const search = (document.getElementById('asset-search').value || '').toLowerCase(); const station = document.getElementById('asset-station').value; const status = document.getElementById('asset-status').value; const filtered = allAssets.filter((asset) => `${asset.id} ${asset.name} ${asset.category}`.toLowerCase().includes(search) && (!station || asset.station === station) && (!status || asset.status === status)); document.getElementById('asset-count').textContent = `${filtered.length} records matching current view`; tbody.innerHTML = filtered.length ? filtered.map((asset) => `<tr><td><div class="asset-name"><span class="asset-icon">+</span><div><strong>${asset.name}</strong><span>${asset.id}</span></div></div></td><td>${asset.category}</td><td>${asset.station}</td><td>${statusBadge(asset.status)}</td><td>${asset.condition}</td><td>${asset.nextMaintenance}</td><td><a class="link-arrow" href="asset-detail.html?id=${asset.id}">View &rarr;</a> <button class="link-arrow delete-asset" data-id="${asset.id}" type="button">Delete</button></td></tr>`).join('') : '<tr><td colspan="7" class="loading">No assets found. Try changing your filters.</td></tr>'; document.querySelectorAll('.delete-asset').forEach((button) => button.addEventListener('click', async () => { const asset = allAssets.find((entry) => entry.id === button.dataset.id); if (asset && window.confirm(`Delete ${asset.name}?`)) { allAssets = await deleteAsset(asset.id); render(); } })); }; ['asset-search', 'asset-station', 'asset-status'].forEach((id) => document.getElementById(id).addEventListener('input', render)); render(); const modal = document.getElementById('asset-modal'); document.getElementById('open-asset-modal').addEventListener('click', () => modal.showModal()); document.getElementById('add-asset-form').addEventListener('submit', async (event) => { event.preventDefault(); allAssets = await getAssets(); await createAsset({ name: document.getElementById('new-asset-name').value, station: document.getElementById('new-asset-station').value, category: document.getElementById('new-asset-category').value }); allAssets = await getAssets(); modal.close(); render(); }); }
async function initAssetDetail() { const target = document.getElementById('asset-detail'); if (!target) return; const asset = await getAsset(getQueryParam('id') || 'GEN-104'); if (!asset) { target.innerHTML = '<div class="card loading">Asset not found.</div>'; return; } target.innerHTML = `<div class="asset-detail-grid"><section class="card detail-hero"><p class="eyebrow">ASSET DETAIL / ${asset.id}</p><h2>${asset.name}</h2><div class="asset-name"><span class="asset-icon">+</span><div><strong>${asset.category} equipment</strong><span>${asset.serialNumber}</span></div>${statusBadge(asset.status)}</div><div class="detail-facts"><div><span>Location</span><strong>${asset.station} Station</strong></div><div><span>Condition</span><strong>${asset.condition}</strong></div><div><span>Last inspection</span><strong>${asset.lastInspection}</strong></div><div><span>Next maintenance</span><strong>${asset.nextMaintenance}</strong></div></div><div class="asset-detail-actions"><button class="btn-primary" id="report-damage">Report damage</button><button class="btn-secondary">Assign asset</button></div></section><section class="card timeline-card"><p class="eyebrow">RECENT LIFECYCLE</p><h2>Movement history</h2><div class="timeline"><div><strong>Inspection completed</strong><small>${asset.lastInspection}</small></div><div><strong>Assigned to ${asset.station}</strong><small>21 Aug 2026</small></div><div><strong>Maintenance completed</strong><small>14 Jul 2026</small></div><div><strong>Received at station</strong><small>03 May 2026</small></div></div></section></div>`; const damageModal = document.getElementById('damage-modal'); document.getElementById('report-damage').addEventListener('click', () => damageModal.showModal()); document.getElementById('damage-form').addEventListener('submit', (event) => { event.preventDefault(); damageModal.close(); alert(`Damage report recorded for ${asset.id}.`); }); }
if (document.getElementById('assets-tbody')) initAssets(); else initAssetDetail();
